import React, { useState } from 'react'
import styles from '../../styles/faq/Question.module.css' // Импортируем модуль стилей

const QuestionTwo = () => {
  // Стейт для отслеживания, раскрыт ли текст
  const [isOpen, setIsOpen] = useState(false)

  // Функция для переключения состояния
  const toggleContent = () => {
    setIsOpen(!isOpen)
  }

  return (
    <div className={styles.container}>
      {/* Заголовок с добавлением стрелочки */}
      <h3 className={styles.mainTitle} onClick={toggleContent}>
        Нужно ли менять домен или IP при блокировке пиcем?{/* Стрелочка, которая меняет угол при раскрытии */}
        <span className={`${styles.arrow} ${isOpen ? styles.open : ''}`}>&#9660;</span>
      </h3>

      {/* Текст, который скрывается или раскрывается */}
      {isOpen && (
        <div>
          <h2>📌 Если ты отправляешь через SMTP чужого сервиса (например Gmail):</h2>
          <p>То дело точно не в твоём домене и IP хостинга. Потому что:</p>
          <ul className={styles.list}>
            <li>
              Письмо уходит с IP <code>smtp.gmail.com</code>
            </li>
            <li>Проверяют репутацию именно аккаунта отправителя (gmail-аккаунта)</li>
            <li>А ещё контент письма и заголовки</li>
            <li>Твой домен и IP хостинга вообще не участвуют в маршрутизации письма</li>
          </ul>

          <h2>📌 Почему письмо может улетать в спам или не доходить:</h2>
          <ol className={styles.list}>
            <li>
              <strong>1️⃣ Плохая репутация аккаунта отправителя (gmail-аккаунта)</strong> → если с него уже много спама или похожих писем — Gmail сам режет его
            </li>
            <li>
              <strong>2️⃣ Плохой или подозрительный контент письма</strong> → слова вроде «бесплатно», «скидка», «заработок», ссылки и вложения с рандомными именами, отсутствие нормального текста в
              теле письма
            </li>
            <li>
              <strong>3️⃣ Отсутствие или кривые заголовки</strong> → отсутствие темы письма, пустое имя отправителя, мусор в вложении
            </li>
            <li>
              <strong>4️⃣ SPF/DKIM/DMARC на домене отправителя</strong> (если ты отправляешь с кастомного адреса типа <code>no-reply@yourdomain.com</code>, а не с <code>gmail.com</code>)
            </li>
          </ol>

          <h2>📌 Что можно сделать:</h2>
          <ul className={styles.list}>
            <li>✅ Попробовать другой gmail-аккаунт</li>
            <li>✅ Изменить тему письма и текст (ближе к деловой переписке)</li>
            <li>✅ Проверить заголовки</li>
            <li>✅ Не прикладывать файлы с рандомными именами</li>
            <li>✅ Проверить репутацию gmail-аккаунта через Gmail Postmaster Tools (если есть доступ)</li>
          </ul>

          <h2>📌 Когда проблема в домене/айпи:</h2>
          <p>Только если ты отправляешь со своего SMTP (mail.yourdomain.com) — тогда:</p>
          <ul className={styles.list}>
            <li>проверяется репутация IP твоего хостинга</li>
            <li>нужны SPF/DKIM/DMARC записи</li>
            <li>могут блочить по IP</li>
          </ul>
          <p>А у тебя всё на чужом SMTP (Gmail) — так что это не про твой домен или айпи.</p>

          <h2>📌 Вывод:</h2>
          <p>
            👉 Спам или отсутствие доставки — это почти всегда контент письма или репутация аккаунта отправителя. Домен и IP твоего хостинга тут вообще не причём, если письмо уходит через{' '}
            <code>smtp.gmail.com</code>.
          </p>
        </div>
      )}
    </div>
  )
}

export default QuestionTwo
